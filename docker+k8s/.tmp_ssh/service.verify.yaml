apiVersion: v1
kind: Service
metadata:
  name: wings-infer-service
  namespace: wings-verify
  labels:
    app: wings-infer
spec:
  type: ClusterIP
  ports:
    - port: 18000
      targetPort: 18000
      protocol: TCP
      name: http
  selector:
    app: wings-infer
